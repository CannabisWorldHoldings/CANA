import { assert, digest, newId, sealPlain } from './core.mjs';
import { assertIndependentVerifier, requirePrincipalReceipt, ACTIONS } from './authority.mjs';
export async function evaluateChallenger({incumbent,challenger,lessonId,cases,evaluator,ablator,verifierId,proposerId,evidenceAdapter,principalReceiptDigest}){
  const lesson=await evidenceAdapter.loadLesson(lessonId);assert(lesson,'canonical lesson not found','RSI_LESSON_NOT_FOUND');assert(lesson.trusted===true&&lesson.status==='ADMITTED'&&lesson.causalStatus==='CAUSALLY_SUPPORTED'&&lesson.admissionDigest&&lesson.valueReceiptDigest,'challenger must consume canonical causally admitted lesson','UNVERIFIED_LESSON_FOR_RSI');
  if(lesson.expiresAt)assert(new Date(lesson.expiresAt).getTime()>=Date.now(),'lesson expired','RSI_LESSON_EXPIRED');assertIndependentVerifier({proposerId,verifierId});await requirePrincipalReceipt(evidenceAdapter,principalReceiptDigest,ACTIONS.PROMOTE_CHALLENGER);assert(typeof evaluator==='function'&&typeof ablator==='function','evaluator and ablator required');assert(Array.isArray(cases)&&cases.length>0,'frozen evaluation cases required');
  const incumbentResult=await evaluator({mechanism:incumbent,cases}),challengerResult=await evaluator({mechanism:challenger,cases}),ablation=await ablator({incumbent,challenger,lesson,cases});const improved=Number(challengerResult.score)>Number(incumbentResult.score),lessonLinked=ablation.lessonContributionEstablished===true&&ablation.lessonId===lesson.lessonId,regressions=Number(challengerResult.criticalRegressions??0);const verdict=improved&&lessonLinked&&regressions===0?'PROMOTE':'REJECT';const body={trialId:newId('rsi'),lessonId:lesson.lessonId,admissionDigest:lesson.admissionDigest,incumbentDigest:digest(incumbent),challengerDigest:digest(challenger),incumbentResult,challengerResult,ablation,verifierId,verdict,createdAt:new Date().toISOString()};return sealPlain({...body,trialDigest:digest(body,'rsi-trial')});
}
export function recursiveImproverCourt({parentImproverDigest,successorImproverDigest,nextCycleReceiptDigest,ablationReceiptDigest}){const pass=Boolean(parentImproverDigest&&successorImproverDigest&&parentImproverDigest!==successorImproverDigest&&nextCycleReceiptDigest&&ablationReceiptDigest);return sealPlain({parentImproverDigest,successorImproverDigest,nextCycleReceiptDigest:nextCycleReceiptDigest??null,ablationReceiptDigest:ablationReceiptDigest??null,verdict:pass?'RECURSIVE_IMPROVEMENT_SUPPORTED':'NOT_ESTABLISHED'});}

import { assert, digest, newId, sealPlain } from './core.mjs';
import { createExperienceCandidate } from './experience-extensions.mjs';
import { resolveCanonicalReceipt } from './receipts.mjs';
export async function compilePageFactoryCandidate({gapReceiptDigest,evidenceAdapter,route,purpose,artifactType='PAGE',contentCandidate,mediaCandidates=[],proposer}){
  const gap=await resolveCanonicalReceipt(evidenceAdapter,gapReceiptDigest,{kind:'INFORMATION_GAP',minimumRealm:'VERIFIED_LOCAL'});assert(route&&purpose&&proposer,'page factory input incomplete','PAGE_FACTORY_INCOMPLETE');assert(['PAGE','BLOG','EDITORIAL'].includes(artifactType),'unsupported artifact type','PAGE_FACTORY_TYPE');const ops=[{type:artifactType==='PAGE'?'ADD_PAGE':'ADD_BLOG',route,purpose,scope:'SINGLE_ROUTE'}];if(contentCandidate)ops.push({type:'UPDATE_CONTENT',contentDigest:contentCandidate.digest??contentCandidate.contentDigest??null,scope:'SINGLE_ROUTE'});for(const media of mediaCandidates)ops.push({type:'REPLACE_IMAGE',mediaDigest:media.digest??media.contentDigest??null,role:media.role??'unspecified',scope:'SINGLE_ROUTE'});const candidate=createExperienceCandidate({objective:`Resolve verified gap: ${gap.payload?.description??gap.receiptDigest}`,target:route,operations:ops,proposer,evidenceReceiptDigests:[gap.receiptDigest]});const result={factoryCandidateId:newId('page_factory'),artifactType,route,purpose,experienceCandidate:candidate,status:'PRIVATE_CANDIDATE_ONLY',publicationAllowed:false};return sealPlain({...result,digest:digest(result,'page_factory')});
}

import { assert, digest, sealPlain } from './core.mjs';
export function buildFrontierLane({lane,frontierEvidence,canaEvidence,mechanism=null,decisiveExperiment=null}){assert(lane,'lane required','FRONTIER_LANE_REQUIRED');const f=frontierEvidence??{},c=canaEvidence??{};const comparable=Boolean(f.benchmarkContractDigest&&c.benchmarkContractDigest&&f.benchmarkContractDigest===c.benchmarkContractDigest&&f.evaluatorDigest&&c.evaluatorDigest&&f.evaluatorDigest===c.evaluatorDigest&&f.window&&c.window&&f.window===c.window&&f.resourceBudgetDigest&&c.resourceBudgetDigest&&f.resourceBudgetDigest===c.resourceBudgetDigest&&f.receiptDigest&&c.receiptDigest);const state={lane,frontierStatus:f.receiptDigest?'EVIDENCED':'UNKNOWN',canaStatus:c.receiptDigest?'EVIDENCED':'UNKNOWN',frontierScore:f.score??null,canaScore:c.score??null,gap:typeof f.score==='number'&&typeof c.score==='number'?f.score-c.score:null,benchmarkComparable:comparable,mechanism,decisiveExperiment,supremacyClaimAllowed:Boolean(comparable&&typeof f.score==='number'&&typeof c.score==='number'&&c.score>f.score)};return sealPlain({...state,digest:digest(state,'frontier_lane')});}

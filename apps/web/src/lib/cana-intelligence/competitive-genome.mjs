import { assert, digest, sealPlain } from './core.mjs';
export function compileCompetitiveGenome(input){assert(input&&typeof input==='object','genome input required','GENOME_INVALID');assert(typeof input.name==='string'&&input.name.trim(),'competitor name required','GENOME_NAME_REQUIRED');const sources=(Array.isArray(input.sources)?input.sources:[]).map((s,i)=>{assert(s&&typeof s==='object',`source ${i} invalid`,'GENOME_SOURCE_INVALID');assert(s.url||s.reference,`source ${i} needs url/reference`,'GENOME_SOURCE_UNGROUNDED');return{id:s.id??`source:${i}`,url:s.url??null,reference:s.reference??null,title:s.title??null,observedAt:s.observedAt??null,realm:s.realm??'PUBLIC_EVIDENCE'};});const sourceIds=new Set(sources.map(s=>s.id));const claims=(Array.isArray(input.claims)?input.claims:[]).map((c,i)=>{const obj=typeof c==='string'?{statement:c,sourceRefs:[]}:c;assert(obj?.statement,`claim ${i} statement required`,'GENOME_CLAIM_INVALID');for(const ref of obj.sourceRefs??[])assert(sourceIds.has(ref),`claim ${i} references unknown source ${ref}`,'GENOME_CLAIM_SOURCE_UNKNOWN');return{id:obj.id??`claim:${i}`,statement:obj.statement,sourceRefs:obj.sourceRefs??[],inference:obj.inference??'DIRECT',confidence:obj.confidence??null,status:(obj.sourceRefs??[]).length?'SUPPORTED_BY_SUPPLIED_EVIDENCE':'UNSUPPORTED'};});const genome={name:input.name.trim(),lane:input.lane??'UNKNOWN',capability:input.capability??null,mechanism:input.mechanism??null,dataAdvantage:input.dataAdvantage??null,workflowAdvantage:input.workflowAdvantage??null,intelligenceAdvantage:input.intelligenceAdvantage??null,experienceAdvantage:input.experienceAdvantage??null,distributionAdvantage:input.distributionAdvantage??null,economics:input.economics??null,moat:input.moat??null,weakness:input.weakness??null,missingCapability:input.missingCapability??null,transferableDNA:input.transferableDNA??[],supersessionVector:input.supersessionVector??null,claims,claimStatus:claims.length&&claims.every(c=>c.status==='SUPPORTED_BY_SUPPLIED_EVIDENCE')?'CLAIMS_EVIDENCED':claims.some(c=>c.status==='SUPPORTED_BY_SUPPLIED_EVIDENCE')?'PARTIAL':'UNSUPPORTED',sources,cleanRoomLaw:'Extract general mechanisms from lawful evidence; never copy proprietary implementation or authority surfaces.'};return sealPlain({...genome,digest:digest(genome,'genome')});}
export function proposeSupersessionChallenger(genome,{mechanism,hypothesis,decisiveExperiment}){assert(genome?.digest,'compiled genome required','GENOME_REQUIRED');assert(mechanism&&hypothesis&&decisiveExperiment,'challenger needs mechanism, hypothesis, decisiveExperiment','GENOME_CHALLENGER_INCOMPLETE');const c={sourceGenomeDigest:genome.digest,mechanism,hypothesis,decisiveExperiment,status:'CANDIDATE_ONLY',authority:'NONE',evidenceCeiling:'UNVERIFIED_UNTIL_COURTED'};return sealPlain({...c,digest:digest(c,'genome_challenger')});}

import { assert, digest, newId, sealPlain } from './core.mjs';
import { EVIDENCE_REALMS } from './receipts.mjs';
export function proposeSkillFromRuns({name,runs,procedure,inputs,outputs,proposer}){assert(name&&Array.isArray(runs)&&procedure&&proposer,'skill proposal incomplete','SKILL_INCOMPLETE');const verified=runs.filter(r=>r.verified===true&&EVIDENCE_REALMS.includes(r.realm)&&!['FIXTURE','SIMULATED','UNKNOWN','REFUSED'].includes(r.realm));const unique=new Map(verified.map(r=>[r.receiptDigest??r.receipt,r]));const criticalFailures=runs.filter(r=>r.criticalFailure===true);assert(unique.size>=3,'need >=3 unique verified successful runs to crystallize a skill candidate','SKILL_EVIDENCE_TOO_SMALL');assert(criticalFailures.length===0,'critical failure blocks skill crystallization','SKILL_CRITICAL_FAILURE');const sourceRuns=[...unique.keys()];const s={skillId:newId('skill'),name,procedure,inputs,outputs,proposer,sourceRuns,status:'CANDIDATE_SKILL',authority:'NONE'};return sealPlain({...s,digest:digest(s,'skill')});}
export function skillPromotionCourt(skill,{hiddenHoldoutReceiptDigest,verifierReceiptDigest}){const pass=Boolean(skill?.digest&&hiddenHoldoutReceiptDigest&&verifierReceiptDigest);return sealPlain({skillDigest:skill?.digest??null,hiddenHoldoutReceiptDigest:hiddenHoldoutReceiptDigest??null,verifierReceiptDigest:verifierReceiptDigest??null,verdict:pass?'ELIGIBLE_FOR_GOVERNED_PROMOTION':'REJECT',authorityMinted:false});}

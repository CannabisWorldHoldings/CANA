# Adversarial court results

The committed tests deny private DNS/IP targets, redirects, proxy/credential injection, wrong content type, oversized/chunked responses, timeouts, invalid JSON, revision/count drift, partial transfer, schema drift, duplicate IDs, cross-tenant evidence, future time, wrong content digest, prohibited predicates, revoked lineage, stale-at-boundary claims, cross-subject frontier aggregation, count-only gap closure, and continuation receipt tamper.

The offline benchmark records one changed acquisition, one unchanged acquisition, one denied revision drift, one denied outage, zero source-failure demotions, zero unsupported admissions, zero false identity links, and zero cognitive promotions. The database-backed migration court separately proves append-only persistence and concurrency.

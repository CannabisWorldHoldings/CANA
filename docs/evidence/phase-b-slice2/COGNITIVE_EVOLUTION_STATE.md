# Cognitive evolution state

`COGNITIVE_PROMOTIONS = 0`. The only implemented cognitive result is the existing `cana-cognitive-reflection-v1` receipt in `reflectVerificationEpisode`. It is hash-bound, deep-frozen, `REFLECTION_ONLY`, `VALUE_NOT_ESTABLISHED`, and routed to `OWNER_REVIEW`.

Slice 2 records a challenger specification because the repeated brittle point is acquisition cost versus independent revalidation. It does not run or select a challenger. Promotion still requires a frozen parent, hidden holdout, adversarial court, negative-transfer tests, independent verification, and later retrieval.

# CANA Armada Weld — Implementation Notes

This overlay is additive. It does **not** create a second truth store, owner authority, database, promotion path, or public API.

## Donor mechanisms welded

- Opus 5 MAX: source trust ceilings, Wraith/tournament discipline, real-traffic causal/economic separation, challenger logic, market-model framing.
- Opus 5: explicit organism cycle and invariant adversary.
- Fable 5: transparent demand × supply × location × price × fulfillment opportunity joins.
- AI Arena donor: retained only as a conceptual/simulation donor; synthetic success mechanisms are intentionally excluded from canonical evidence paths.

## New additive kernel

- `epistemics.mjs`: source ceilings and synthetic containment.
- `reality-graph.mjs`: temporal evidence-resolved entity projection.
- `demand-graph.mjs`: observed-interaction-only demand aggregation.
- `opportunity.mjs`: evidence-bearing market-gap discovery.
- `prediction.mjs`: immutable falsifiable prediction locks.
- `experiment.mjs`: preregistration, verified owner authorization, observed settlement.
- `economics.mjs`: ValueReceipts that refuse dollar claims without causal support + observed AOV.
- `memory.mjs`: independent lesson admission.
- `armada.mjs`: a provider-neutral META-ARMADA contract that emits an intelligence-allocation receipt. The checked-in command runner is fixture-only and accepts only source-registered adapters; adding a real provider adapter requires a reviewed source change and separate provider authority.
- `wraith.mjs`: independent adversarial attack court.
- `rsi.mjs`: lesson-linked incumbent/challenger evaluation with ablation and independent verification.
- `audit.mjs`: constitutional invariant auditor.
- `canonical-adapter.mjs`: explicit seam to canonical CANA Prisma/auth/reality; kernel owns no authority.
- `organism-cycle.mjs`: read-only PERCEIVE → UNDERSTAND → DISCOVER → VERIFY cycle that stops at the authority gate.

## Intentionally not implemented here

This reconciled implementation does not claim:

- actual OpenAI/Anthropic/Gemini/OpenRouter model-provider wiring;
- real ORDERWEEDDC traffic;
- production deployment;
- economic or causal success.

The retained donor apply script is forensic installation material only and is not a canonical promotion path.

## Command execution boundary

`armada-runner.mjs` cannot accept a repository root, command, argument vector, working directory, environment, inherited-secret list, provider/model/process identity, shell, Git action, SQL action, or deployment primitive from JSON. The canonical repository is derived from the source-owned runner location, verified against Git's top level, and the requested base commit must be reachable from its current `HEAD`. Candidate and verifier processes resolve through the closed source registry in `command-executor.mjs`; that registry owns provider/model/verifier identity plus a portable implementation identity, and derives the process-identity digest without machine-local executable paths. Duplicate candidate ids or implementation identities fail before any worktree is created. Git custody uses a closed environment with global/system configuration and hooks disabled. Processes run in separate disposable worktrees at an exact 40-character commit SHA, receive no inherited `HOME`, and carry `CANA_ARMADA_EFFECT_AUTHORITY=NONE`. The current registry contains fixture adapters only. The independent verifier is also isolated and must leave its worktree unchanged. Its receipt must be a new file outside the source repository, so a caller cannot overwrite source or an existing receipt.

## Protected-champion installation law

The top-level installer never writes into the protected champion. It verifies exact HEAD/tree and cleanliness, creates a detached worktree, applies the controlled Next 15.5.21 + `middleware.ts` Option-A transformation only in that descendant, then copies the additive Armada kernel there. Direct overlay application to the champion is disabled.

# Mission 2 blocker and repair ledger

Every entry is technical and remained inside the automatic-completion boundary.
No test, security, privacy, truth, freshness, or fail-closed rule was weakened.

| ID | Reproduction | Classification | Root cause | Narrow repair | Regression evidence |
|---|---|---|---|---|---|
| `M2-B001` | First Mission 2 unit run: 17 tests, 10 passed, 7 failed | deterministic implementation defect | first event dereferenced a null prior projection | require an existing projection before transition validation | durable reconstruction and illegal-transition courts pass |
| `M2-B002` | Second unit run: 17 tests, 12 passed, 5 failed | deterministic implementation defect | reconstructed missions did not copy tenant/workspace from the event envelope | bind tenant/workspace from the first event and reject later drift | cross-tenant and restart courts pass |
| `M2-B003` | Second unit run rejected an approved promotion payload | deterministic serialization defect | the approved branch serialized `failure: undefined` | omit the field unless a real failure exists | complete promoted lifecycle court passes |
| `M2-B004` | Initial promoted documentation bytes differed from the branch by one blank line | evidence/receipt mismatch | generator and applied Markdown spacing differed | make the generator include the canonical blank line and regenerate all evidence | branch file SHA-256 equals `approved_after_sha256` |
| `M2-B005` | Combined workflow/unit run while the workflow files were dirty: 26 tests, 25 passed, 1 failed | verification precondition, not candidate semantics | the offline GitHub preparer correctly refuses dirty source | commit the workflow change, then rerun from a clean tree | clean run passes 26/26 |
| `M2-B006` | deletion of the final event originally reconstructed a shorter apparently valid chain | real durability defect | hash chaining alone cannot distinguish an intentional prefix from truncated history | compare reconstructed event count and tail hash with the last atomic projection checkpoint | deletion now returns `EVENT_DELETION_DETECTED` |
| `M2-B007` | first ownership-regression run: 35 tests, 16 passed, 19 failed | deterministic ownership-policy defect | the authorization-effect value did not contain the validator's exact fail-closed `no production` phrase | make every denied authority explicit and recompute the sealed assignment digest | ownership regression run passes 35/35 |
| `M2-B008` | isolated Node 24 build-database court: 27 tests, 26 passed, 1 failed | deterministic Node 24 test-cleanup defect | `fs.rmSync` refuses a symlink to a directory with `ERR_FS_EISDIR`, so the deliberately replaced root was not restored before fail-closed workspace cleanup | observe the rejection through an async callback and unlink the symlink explicitly before restoring the owned root | isolated build-database court passes without weakening the symlink-root rejection |
| `M2-B009` | independent context review at `73ca0b03e86f8839c7079e401e30b11daadd4818` | exact ownership evidence defect | the Node 24 fixture repair remained covered by the canonical PR #2 assignment but was absent from Mission 2's exact changed-path list | add only `apps/web/tests/build-database-gate.test.mjs` to the Mission 2 assignment and reseal its digest | exact base-to-head changed-path comparison and ownership courts must pass on the new SHA |
| `M2-B010` | independent security and code review forged canonical authorization and lease objects at `73ca0b03e86f8839c7079e401e30b11daadd4818` | real authority-boundary defect | canonical hashes proved integrity but not issuance by CANA | add exact receipt schemas plus module-private admission for authorizations and hash-bound worker leases; require admitted grants at dispatch and execution | forged authorization, cloned lease, stale lease, expiry, and paused-preauthorization courts pass |
| `M2-B011` | independent verifier review supplied coherent forged execution and verifier receipts | real promotion-boundary defect | the verifier and kernel trusted caller-declared receipt fields and hashes without recomputing repository reality or receipt provenance | independently recompute Git/source/diff/operation/buffers/effects and require admitted exact execution, verifier, and rollback receipts | forged hashes, commands, byte buffers, cloned receipts, self-verification, and forged rollback courts fail closed |
| `M2-B012` | coordinated deletion of the last event and matching projection reconstructed a shorter valid prefix | real durability defect | the projection could be deleted or edited with the same event tail and was not an independent durable anchor | add an atomically sealed `head.json`, fsync event/head/projection ordering, and repair only verified log-ahead crash windows | projection-behind recovery passes; event-plus-projection truncation and head-ahead corruption fail closed |
| `M2-B013` | evidence path and store-root symlink courts redirected writes outside the requested store | real evidence-boundary defect | canonical path containment did not reject every symlinked root, parent, directory, file, or replacement race | resolve a canonical owned root, use no-follow opened-file identity checks, exclusive writes, canonical parents, and directory fsync | root, evidence directory, evidence target, replaced parent, and content corruption courts fail closed |
| `M2-B014` | a paused mission could be dispatched because dispatch checked only a prior authorization receipt | real lifecycle defect | dispatch eligibility was not bound to the current lifecycle state | require current state `CANA_AUTHORIZED`, persist the resume state, and clear it only after a valid resume | paused-preauthorization dispatch and illegal-transition courts fail closed |
| `M2-B015` | retry metadata recorded backoff but immediate resume still succeeded; owner-decision state had no reachable transition | real autonomy-control defect | backoff was documentary and the state graph omitted `DEAD_LETTER` to `OWNER_DECISION_REQUIRED` | persist and enforce `retry_not_before`, bound attempts, restore the authorized resume state, and add the explicit terminal owner-decision transition | immediate retry is denied, elapsed retry succeeds, exhaustion is durable, and owner-decision state is reached |
| `M2-B016` | independent context review found a hard-coded adversarial court count/name list and asserted no-effect fields | evidence-generation defect | the report described expected fixtures instead of deriving all conclusions from executed courts | derive court inventory, event deltas, repository/file hashes, promotion state, and effect counts from two deterministic executions of every invalid court | generated report contains 18 executed courts, reproducible denials, zero unauthorized execution, zero promotion/memory, and measured zero effects |
| `M2-B017` | adding the new exact lease authority module caused durability ownership to omit one Mission 2 path | exact ownership evidence defect | the original sealed set predated the dedicated lease module | add only `tools/mission-2/lease.mjs` to the exact assignment, create list, and planned-candidate list; reseal both ownership digests | ownership suite passes 35/35 and neighboring paths remain denied |
| `M2-B018` | first exact-SHA code review at `3ade13324a6797b29dc54ce76d09dee66ebb4a22` appended an initial `CANA_AUTHORIZED` event directly | real authority-boundary defect | the generic durable append surface allowed a lifecycle-shaped first event without the required predecessor chain or content-addressed authorization evidence | require every chain to begin at `SIGNAL_OBSERVED`; persist the exact authorization evidence reference and revalidate its receipt at dispatch | direct first authorization and a forged lifecycle-shaped authorization chain both fail closed |
| `M2-B019` | first exact-SHA code review rewrote both the event prefix and unkeyed co-located head | real durability defect | an ordinary hash did not distinguish a legitimate head from a coordinated attacker-recomputed head | authenticate the head with an external private HMAC key created exclusively outside the mutable store root | unkeyed rewritten head, missing key, key symlink, and coordinated tail deletion courts fail closed |
| `M2-B020` | first exact-SHA code review left `append.lock` behind and restarted the store | real restart defect | an existence-only lock could not distinguish an active writer from a dead process | write a canonical lock receipt with PID and nonce, recover only a proven dead owner, and verify lock inode before release | dead-owner lock recovers; live and replaced locks fail closed |
| `M2-B021` | first exact-SHA goal, code, and security reviews serialized authorization, lease, execution, verifier, and rollback receipts across process boundaries | real restart and independent-verification defect | module-private object-identity admission could not survive restart and made the independent verifier process-dependent | replace object identity with exact-schema, canonical-hash, source, time, executor/verifier, durable-evidence, and causal receipt validation | Node 24 fresh-process receipt validation plus serialized lifecycle, tampering, rollback, and evidence-regeneration courts pass |
| `M2-B022` | exact-SHA code review at `940fdcbf966155da2c298d3ef1ffc31bdefe89e6` supplied self-hashed policy-divergent authorization and invented all-true verifier approval receipts | real authorization and promotion defect | receipt integrity validation alone did not prove that CANA policy or the independent verifier produced the submitted decision | load durable sealed mission/context and rerun deterministic authorization; independently rerun live sandbox falsification at recording and promotion; require exact reproduced receipts | recomputed forged authorization and coherent forged verifier approval fail closed while serialized genuine receipts still complete the lifecycle |
| `M2-B023` | exact-SHA code review at `f3a05df60e47d1aa1b69b23f964d89f6c1de9a6d` faulted the mock rewrite between destructive file operations | real interruption/rollback defect | the adapter truncated the only target inode before replacement bytes and the checkpoint retained hashes rather than exact recovery bytes | fsync a content-bound exact-byte recovery journal, fsync a same-filesystem temp, atomically rename, fsync the directory, and make execute/rollback/reapply idempotent across both atomic states | interruption before rename, after rename, after rollback, and after reapply resumes without partial bytes and reproduces the exact receipt |
| `M2-B024` | exact-SHA code review admitted malformed Source Records and under-specified Foundry record types | real schema-validation defect | one shallow common validator did not enforce the ten type-specific contracts or their relationships | add exact per-type field allowlists, required types, truth-state constraints, stable whole-record identities, and same-tenant typed reference checks | every valid type passes; removal of a defining field, unknown fields, missing/wrong references, commercial-value claims, and forbidden truth transitions fail closed |
| `M2-B025` | exact-SHA security review supplied a coherent self-hashed lease with attacker-selected token and expiry | real pre-execution authority defect | the executor checked only an attacker-recomputable lease digest before mutation | sign lease bodies with a store-specific Ed25519 CANA authority and require the trusted public key at executor, kernel, restart, heartbeat, and verifier boundaries | coherent self-hashed lease forgery returns `LEASE_AUTHENTICITY_DENIED`; serialized genuine leases remain restart-safe |
| `M2-B026` | exact-SHA security review found verifier recomputation occurred in the CANA process | real independence defect | identity separation did not prove a separate runtime state boundary | serialize immutable inputs to a dedicated verifier child process with a scrubbed environment; require receipts to declare the separate-process boundary and rerun that process at recording and promotion | in-process verification is denied, executor self-verification fails closed, and independent child-process receipts reproduce exactly |
| `M2-B027` | exact-SHA code review at `b1ed30c8eb0d72e2095b5069a394650464d6b23e` advanced the admission clock after a valid independent receipt | real restart/promotion defect | CANA reproduced a time-bound receipt using the later wall clock rather than its recorded verification instant | require the receipt time to follow execution, remain inside the sealed mission window, not exceed the admission clock, and rerun independent verification at that recorded instant | a genuine serialized receipt admits and promotes after elapsed time while future and altered receipts fail closed |
| `M2-B028` | exact-SHA code/security review substituted verification operation and success text after execution | real immutable-evidence defect | verifier propositions lived only in caller-supplied transient context | add an exact mission field allowlist, seal the exact operation and success text in `verification_contract`, and restrict runtime admission context to sandbox plus authenticated lease | unknown mission fields, operation substitution, success-text substitution, and admission-context extension fail closed |
| `M2-B029` | exact-SHA code/security review supplied a coherent self-hashed rollback receipt while approved bytes remained applied | real rollback-authority defect | receipt validation checked declarations and a recomputable hash but did not reopen the restored target | require the no-symlink sandbox target to byte-equal the execution receipt's exact pre-mission bytes before recording rollback | a coherent false rollback receipt returns `ROLLBACK_STATE_MISMATCH`; exact executor rollback still records and survives serialization |

Failed results were not retried until lucky. Inputs changed before every rerun, and
the final clean runs execute normally in fresh processes.

No owner decision was required. Hermes, provider, spend, production, hosting, DNS,
credentials, deployment, outreach, and public communication remained untouched.

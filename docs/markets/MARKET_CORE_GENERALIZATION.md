# MARKET CORE GENERALIZATION — the fork ×2 lane, judged

**The scientific question:** can repeated state entry systematically remove code,
time, and human work from the next state WITHOUT weakening truth?

**Answer: YES — measured.**

## The evidence-threshold law, obeyed
Generalization happened exactly at fork ×2 (VA + MD proved the shape), never
before (DC's ArcGIS adapter remains n=1 of its class and was NOT touched).

## What was extracted
| New shared core | LOC | Replaces |
|---|---|---|
| `reality/bounded-html-acquisition.mjs` | 214 | the duplicated acquisition law set (fixed origin, operator opt-in, pinned lookup, bounds, double-fetch stability, receipt shape) |
| `markets/market-claims.mjs` | 114 | the duplicated claim-formation law set (UNKNOWN default, versioned name+address identity, license-absence UNKNOWN, sorted frozen claims) |

## What each market now owns (identity only)
| File | Before | After |
|---|---|---|
| `live-va-cca-adapter.mjs` | 215 | **50** (−77%) |
| `live-md-mca-adapter.mjs` | 195 | **48** (−75%) |
| `va-claims.mjs` | 215 | **122** (delivery honesty law stays VA-specific) |
| `md-claims.mjs` | 91 | **35** (−62%) |
| **Total (4 files + 2 cores)** | **716** | **583 (−133 net, while adding reusable capacity)** |

## Truth NOT weakened — the proof
- **All 64 tests pass UNCHANGED** — not one assertion edited. Same laws, same
  error codes, same receipt shapes, same digests (contracts stayed literal in
  their files, so every contract digest is byte-identical).
- **D.C. untouched:** zero DC-lane files modified; the ABCA equivalence court
  (happy path + 15 hostile mutations) green.
- **Court regex provenance preserved:** version literals remain in the exact
  files the execution-provenance maps read at the pinned commit.

## Market #4's projected cost (falsifiable at the next transfer test)
Parser (source-shaped) + ~50-line adapter contract + ~35-line claim parameters
+ four additive registrations + lane script + fixtures + one verified-world-state
test. No core changes of any kind expected.

## Lane clock
14:21:45Z start → 14:24:37Z full court green: **under 3 minutes.**

## Compiler learning curve to date (all measured 2026-08-12)
| n | Market | Extracted coverage | Court-proven VERIFIED | Core changes |
|---|---|---|---|---|
| 1 | DC | (pre-existing, live production data) | production | — |
| 2 | VA | ~22 min | same-day | 2 court fns generalized |
| 3 | MD | ~4 min | ~7 min | ZERO (registrations only) |
| — | fork×2 lane | — | re-court <3 min | −133 LOC, per-market code −60..77% |
